/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.newline53.sdk.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ListTransfersStatus
 * 
 * <p>A value indicating the overall status of the Transfer.
 */
public class ListTransfersStatus {

    public static final ListTransfersStatus CANCELED = new ListTransfersStatus("canceled");
    public static final ListTransfersStatus QUEUED = new ListTransfersStatus("queued");
    public static final ListTransfersStatus PENDING = new ListTransfersStatus("pending");
    public static final ListTransfersStatus SETTLED = new ListTransfersStatus("settled");
    public static final ListTransfersStatus FAILED = new ListTransfersStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListTransfersStatus> values = createValuesMap();
    private static final Map<String, ListTransfersStatusEnum> enums = createEnumsMap();

    private final String value;

    private ListTransfersStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ListTransfersStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListTransfersStatus
     */ 
    @JsonCreator
    public static ListTransfersStatus of(String value) {
        synchronized (ListTransfersStatus.class) {
            return values.computeIfAbsent(value, v -> new ListTransfersStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListTransfersStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListTransfersStatus other = (ListTransfersStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListTransfersStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListTransfersStatus[] values() {
        synchronized (ListTransfersStatus.class) {
            return values.values().toArray(new ListTransfersStatus[] {});
        }
    }

    private static final Map<String, ListTransfersStatus> createValuesMap() {
        Map<String, ListTransfersStatus> map = new LinkedHashMap<>();
        map.put("canceled", CANCELED);
        map.put("queued", QUEUED);
        map.put("pending", PENDING);
        map.put("settled", SETTLED);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, ListTransfersStatusEnum> createEnumsMap() {
        Map<String, ListTransfersStatusEnum> map = new HashMap<>();
        map.put("canceled", ListTransfersStatusEnum.CANCELED);
        map.put("queued", ListTransfersStatusEnum.QUEUED);
        map.put("pending", ListTransfersStatusEnum.PENDING);
        map.put("settled", ListTransfersStatusEnum.SETTLED);
        map.put("failed", ListTransfersStatusEnum.FAILED);
        return map;
    }
    
    
    public enum ListTransfersStatusEnum {

        CANCELED("canceled"),
        QUEUED("queued"),
        PENDING("pending"),
        SETTLED("settled"),
        FAILED("failed"),;

        private final String value;

        private ListTransfersStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

