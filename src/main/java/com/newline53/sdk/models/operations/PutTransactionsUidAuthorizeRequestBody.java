/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.newline53.sdk.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.newline53.sdk.utils.Utils;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class PutTransactionsUidAuthorizeRequestBody {
    /**
     * The current status of the transaction authorization.
     */
    @JsonProperty("authorization_status")
    private AuthorizationStatusRequest authorizationStatus;

    /**
     * Freeform text (up to 44 characters) that should be attached to the transaction if the transaction is
     * to be denied. Optional.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("denied_memo")
    private String deniedMemo;

    /**
     * The reason for the authorization denial. For wire transactions, must be one of (`vrn_archived`),
     * (`vrn_locked`), (`international_payment`), (`unauthorized_credit`). For ACH transactions, must be
     * one of (`vrn_archived`), (`vrn_locked`), (`debit_limit_exceeded`), (`unauthorized_credit`),
     * (`unauthorized_debit`), (`insufficient_funds`)
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("denied_reason")
    private DeniedReason deniedReason;

    @JsonCreator
    public PutTransactionsUidAuthorizeRequestBody(
            @JsonProperty("authorization_status") @Nonnull AuthorizationStatusRequest authorizationStatus,
            @JsonProperty("denied_memo") @Nullable String deniedMemo,
            @JsonProperty("denied_reason") @Nullable DeniedReason deniedReason) {
        this.authorizationStatus = Optional.ofNullable(authorizationStatus)
            .orElseThrow(() -> new IllegalArgumentException("authorizationStatus cannot be null"));
        this.deniedMemo = deniedMemo;
        this.deniedReason = deniedReason;
    }
    
    public PutTransactionsUidAuthorizeRequestBody(
            @Nonnull AuthorizationStatusRequest authorizationStatus) {
        this(authorizationStatus, null, null);
    }

    /**
     * The current status of the transaction authorization.
     */
    public AuthorizationStatusRequest authorizationStatus() {
        return this.authorizationStatus;
    }

    /**
     * Freeform text (up to 44 characters) that should be attached to the transaction if the transaction is
     * to be denied. Optional.
     */
    public Optional<String> deniedMemo() {
        return Optional.ofNullable(this.deniedMemo);
    }

    /**
     * The reason for the authorization denial. For wire transactions, must be one of (`vrn_archived`),
     * (`vrn_locked`), (`international_payment`), (`unauthorized_credit`). For ACH transactions, must be
     * one of (`vrn_archived`), (`vrn_locked`), (`debit_limit_exceeded`), (`unauthorized_credit`),
     * (`unauthorized_debit`), (`insufficient_funds`)
     */
    public Optional<DeniedReason> deniedReason() {
        return Optional.ofNullable(this.deniedReason);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The current status of the transaction authorization.
     */
    public PutTransactionsUidAuthorizeRequestBody withAuthorizationStatus(@Nonnull AuthorizationStatusRequest authorizationStatus) {
        this.authorizationStatus = Utils.checkNotNull(authorizationStatus, "authorizationStatus");
        return this;
    }


    /**
     * Freeform text (up to 44 characters) that should be attached to the transaction if the transaction is
     * to be denied. Optional.
     */
    public PutTransactionsUidAuthorizeRequestBody withDeniedMemo(@Nullable String deniedMemo) {
        this.deniedMemo = deniedMemo;
        return this;
    }


    /**
     * The reason for the authorization denial. For wire transactions, must be one of (`vrn_archived`),
     * (`vrn_locked`), (`international_payment`), (`unauthorized_credit`). For ACH transactions, must be
     * one of (`vrn_archived`), (`vrn_locked`), (`debit_limit_exceeded`), (`unauthorized_credit`),
     * (`unauthorized_debit`), (`insufficient_funds`)
     */
    public PutTransactionsUidAuthorizeRequestBody withDeniedReason(@Nullable DeniedReason deniedReason) {
        this.deniedReason = deniedReason;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PutTransactionsUidAuthorizeRequestBody other = (PutTransactionsUidAuthorizeRequestBody) o;
        return 
            Utils.enhancedDeepEquals(this.authorizationStatus, other.authorizationStatus) &&
            Utils.enhancedDeepEquals(this.deniedMemo, other.deniedMemo) &&
            Utils.enhancedDeepEquals(this.deniedReason, other.deniedReason);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            authorizationStatus, deniedMemo, deniedReason);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PutTransactionsUidAuthorizeRequestBody.class,
                "authorizationStatus", authorizationStatus,
                "deniedMemo", deniedMemo,
                "deniedReason", deniedReason);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private AuthorizationStatusRequest authorizationStatus;

        private String deniedMemo;

        private DeniedReason deniedReason;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * The current status of the transaction authorization.
         */
        public Builder authorizationStatus(@Nonnull AuthorizationStatusRequest authorizationStatus) {
            this.authorizationStatus = Utils.checkNotNull(authorizationStatus, "authorizationStatus");
            return this;
        }

        /**
         * Freeform text (up to 44 characters) that should be attached to the transaction if the transaction is
         * to be denied. Optional.
         */
        public Builder deniedMemo(@Nullable String deniedMemo) {
            this.deniedMemo = deniedMemo;
            return this;
        }

        /**
         * The reason for the authorization denial. For wire transactions, must be one of (`vrn_archived`),
         * (`vrn_locked`), (`international_payment`), (`unauthorized_credit`). For ACH transactions, must be
         * one of (`vrn_archived`), (`vrn_locked`), (`debit_limit_exceeded`), (`unauthorized_credit`),
         * (`unauthorized_debit`), (`insufficient_funds`)
         */
        public Builder deniedReason(@Nullable DeniedReason deniedReason) {
            this.deniedReason = deniedReason;
            return this;
        }

        public PutTransactionsUidAuthorizeRequestBody build() {
            return new PutTransactionsUidAuthorizeRequestBody(
                authorizationStatus, deniedMemo, deniedReason);
        }

    }
}
