/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.newline53.sdk.models.operations;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * DeniedReason
 * 
 * <p>The reason for the authorization denial. For wire transactions, must be one of (`vrn_archived`),
 * (`vrn_locked`), (`international_payment`), (`unauthorized_credit`). For ACH transactions, must be
 * one of (`vrn_archived`), (`vrn_locked`), (`debit_limit_exceeded`), (`unauthorized_credit`),
 * (`unauthorized_debit`), (`insufficient_funds`)
 */
public enum DeniedReason {
    DEBIT_LIMIT_EXCEEDED("debit_limit_exceeded"),
    INSUFFICIENT_FUNDS("insufficient_funds"),
    INTERNATIONAL_PAYMENT("international_payment"),
    UNAUTHORIZED_CREDIT("unauthorized_credit"),
    UNAUTHORIZED_DEBIT("unauthorized_debit"),
    VRN_ARCHIVED("vrn_archived"),
    VRN_LOCKED("vrn_locked");

    @JsonValue
    private final String value;

    DeniedReason(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<DeniedReason> fromValue(String value) {
        for (DeniedReason o: DeniedReason.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

